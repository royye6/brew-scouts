import React from "react";
import BannerText from "../ui/BannerText";

const Banner = () => {
    return (
        <div className="w-full h-12 bg-green-100 flex items-center justify-center text-sm font-semibold text-center">
            <BannerText />
        </div>
    );
};

export default Banner;
